import React from 'react'
import {FaHireAHelper} from 'react-icons/fa'

const Icons = (props) => {
    console.log(props)
  return (
    // <h1>{props.icon}</h1>
    <i>{props.icon}</i>
  )

}

export default Icons