import React from 'react'

const Input = (props) => {
  return (
    
        <input type="search" onChange={props.onChang
        } style={{
            borderRadius:"10px"
        }}></input>
    
  )
}

export default Input